[project]
name = "context-manager-mcp-server"
version = "1.0.0"
description = "MCP Server for Context Manager - exposes contexts to Claude Code"
requires-python = ">=3.10"
dependencies = [
    "mcp>=0.9.0",
]

[project.scripts]
context-manager-mcp = "server:main"
